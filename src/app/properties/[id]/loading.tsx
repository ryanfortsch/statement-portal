import { HelmLoading } from '@/components/HelmLoading';

export default function PropertyDetailLoading() {
  return <HelmLoading current="work" eyebrow="Property" headlineWidth={320} contentRows={4} />;
}
