import { HelmLoading } from '@/components/HelmLoading';

export default function RevenueLoading() {
  return <HelmLoading current="revenue" eyebrow="Helm · Revenue" headlineWidth={440} contentRows={7} />;
}
