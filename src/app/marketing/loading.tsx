import { HelmLoading } from '@/components/HelmLoading';

export default function MarketingLoading() {
  return <HelmLoading current="marketing" eyebrow="Helm · Marketing" headlineWidth={420} contentRows={5} />;
}
